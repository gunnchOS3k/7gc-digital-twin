# Figures

No hand-drawn RF plots. Tables under `paper/tables/` are generated by `paper/scripts/generate_tables.py` from experiment JSON (`SYNTHETIC_SIM`). Additional figures require a generator script and must not be typed into the PDF.
