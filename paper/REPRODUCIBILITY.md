# Reproducibility (Paper I)

Canonical: [../REPRODUCIBILITY.md](../REPRODUCIBILITY.md)

```bash
make paper-reproduce
```

Outputs: `results/experiments/rq1_gary_flagship_profiles.json` (gitignored) and `paper/tables/rq1_runs.tex` (committed, SYNTHETIC_SIM). Independent human reproduction remains PENDING.
