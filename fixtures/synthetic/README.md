# Synthetic fixtures

Deterministic JSON used by RQ1 tests. These are **not** field measurements and **not** community-deployment records.

- `gary_users.json` — small synthetic demand/mobility set for schema and metric-family tests.
