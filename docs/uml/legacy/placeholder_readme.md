# Legacy — placeholder README

Prior content of `docs/uml/README.md` was:

> See repo-specific architecture docs. SpectrumX: full UML pack.

That sentence is **not** citable as the architecture of this repository. Current diagrams live under `docs/uml/current/` and are generated from this tree's Python/YAML, copying SpectrumX **discipline** (current / future / legacy / rendered / traceability) rather than SpectrumX diagrams.
